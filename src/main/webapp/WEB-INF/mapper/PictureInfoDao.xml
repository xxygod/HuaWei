<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gangOfFour.shopping.dao.PictureInfoDao">
    <insert id="insertPictureInfo" useGeneratedKeys="true" keyProperty="pictureId" keyColumn="pictureId">
      INSERT INTO pictureInfo(productId,productUrl,pictureOrder) VALUES(${productInfo.productId},#{productUrl},${pictureOrder})
    </insert>
</mapper>